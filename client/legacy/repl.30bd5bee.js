import './web.url.fd241fe0.js';
import { S as SvelteComponentDev, i as init, d as dispatch_dev, s as safe_not_equal, v as validate_slots, E as stores$1, o as onMount, O as onDestroy, t as track, f as element, l as claim_element, m as children, h as detach_dev, r as attr_dev, p as add_location, u as insert_dev, y as listen_dev, X as prop_dev, b as binding_callbacks, a as bind, c as space, e as create_component, g as text, q as query_selector_all, j as claim_space, k as claim_component, n as claim_text, Q as set_style, w as mount_component, x as append_dev, Y as prevent_default, z as add_flush_callback, A as transition_in, B as transition_out, C as destroy_component, Z as run_all, N as globals, T as set_data_dev, H as noop } from './client.65c31bad.js';
import { M as Markmap_1, a as lodash_debounce, t as transform$2, g as getUsedAssets, f as fillTemplate } from './markmap.235e0705.js';
import { H as Header, F as Footer } from './footer.1ccdbf46.js';
import { T as Toolbar_1, s as subscribeHash } from './gist.7d585f9e.js';

/* src/routes/repl.svelte generated by Svelte v3.29.4 */

const { document: document_1 } = globals;
const file = "src/routes/repl.svelte";

// (154:4) {:else}
function create_else_block(ctx) {
	let form;
	let span;
	let t0;
	let t1;
	let input;
	let input_value_value;
	let t2;
	let a0;
	let t3;
	let t4;
	let a1;
	let t5;
	let t6;
	let mounted;
	let dispose;
	let if_block = /*gist*/ ctx[5].error && create_if_block_2(ctx);

	const block = {
		c: function create() {
			form = element("form");
			span = element("span");
			t0 = text("Open Gist:");
			t1 = space();
			input = element("input");
			t2 = space();
			a0 = element("a");
			t3 = text("✓");
			t4 = space();
			a1 = element("a");
			t5 = text("✗");
			t6 = space();
			if (if_block) if_block.c();
			this.h();
		},
		l: function claim(nodes) {
			form = claim_element(nodes, "FORM", { class: true });
			var form_nodes = children(form);
			span = claim_element(form_nodes, "SPAN", { class: true });
			var span_nodes = children(span);
			t0 = claim_text(span_nodes, "Open Gist:");
			span_nodes.forEach(detach_dev);
			t1 = claim_space(form_nodes);

			input = claim_element(form_nodes, "INPUT", {
				class: true,
				style: true,
				type: true,
				value: true
			});

			t2 = claim_space(form_nodes);
			a0 = claim_element(form_nodes, "A", { class: true });
			var a0_nodes = children(a0);
			t3 = claim_text(a0_nodes, "✓");
			a0_nodes.forEach(detach_dev);
			t4 = claim_space(form_nodes);
			a1 = claim_element(form_nodes, "A", { class: true });
			var a1_nodes = children(a1);
			t5 = claim_text(a1_nodes, "✗");
			a1_nodes.forEach(detach_dev);
			t6 = claim_space(form_nodes);
			if (if_block) if_block.l(form_nodes);
			form_nodes.forEach(detach_dev);
			this.h();
		},
		h: function hydrate() {
			attr_dev(span, "class", "mr-2");
			add_location(span, file, 155, 6, 4223);
			attr_dev(input, "class", "input min-w-0");
			set_style(input, "width", "560px");
			attr_dev(input, "type", "url");
			input.value = input_value_value = /*gist*/ ctx[5].url;
			add_location(input, file, 156, 6, 4266);
			attr_dev(a0, "class", "ml-2 no-underline");
			add_location(a0, file, 157, 6, 4397);
			attr_dev(a1, "class", "ml-2 no-underline");
			add_location(a1, file, 158, 6, 4486);
			attr_dev(form, "class", "flex");
			add_location(form, file, 154, 4, 4155);
		},
		m: function mount(target, anchor) {
			insert_dev(target, form, anchor);
			append_dev(form, span);
			append_dev(span, t0);
			append_dev(form, t1);
			append_dev(form, input);
			append_dev(form, t2);
			append_dev(form, a0);
			append_dev(a0, t3);
			append_dev(form, t4);
			append_dev(form, a1);
			append_dev(a1, t5);
			append_dev(form, t6);
			if (if_block) if_block.m(form, null);

			if (!mounted) {
				dispose = [
					listen_dev(input, "input", /*input_handler*/ ctx[13], false, false, false),
					listen_dev(a0, "click", prevent_default(/*handleGistOpen*/ ctx[11]), false, true, false),
					listen_dev(a1, "click", prevent_default(/*handleGistCancel*/ ctx[12]), false, true, false),
					listen_dev(form, "submit", prevent_default(/*handleGistOpen*/ ctx[11]), false, true, false)
				];

				mounted = true;
			}
		},
		p: function update(ctx, dirty) {
			if (dirty & /*gist*/ 32 && input_value_value !== (input_value_value = /*gist*/ ctx[5].url)) {
				prop_dev(input, "value", input_value_value);
			}

			if (/*gist*/ ctx[5].error) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block_2(ctx);
					if_block.c();
					if_block.m(form, null);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(form);
			if (if_block) if_block.d();
			mounted = false;
			run_all(dispose);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_else_block.name,
		type: "else",
		source: "(154:4) {:else}",
		ctx
	});

	return block;
}

// (152:4) {#if !gist}
function create_if_block_1(ctx) {
	let a;
	let t;
	let mounted;
	let dispose;

	const block = {
		c: function create() {
			a = element("a");
			t = text("Open Gist");
			this.h();
		},
		l: function claim(nodes) {
			a = claim_element(nodes, "A", {});
			var a_nodes = children(a);
			t = claim_text(a_nodes, "Open Gist");
			a_nodes.forEach(detach_dev);
			this.h();
		},
		h: function hydrate() {
			add_location(a, file, 152, 4, 4080);
		},
		m: function mount(target, anchor) {
			insert_dev(target, a, anchor);
			append_dev(a, t);

			if (!mounted) {
				dispose = listen_dev(a, "click", prevent_default(/*handleGistInput*/ ctx[10]), false, true, false);
				mounted = true;
			}
		},
		p: noop,
		d: function destroy(detaching) {
			if (detaching) detach_dev(a);
			mounted = false;
			dispose();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_1.name,
		type: "if",
		source: "(152:4) {#if !gist}",
		ctx
	});

	return block;
}

// (160:6) {#if gist.error}
function create_if_block_2(ctx) {
	let span;
	let t_value = /*gist*/ ctx[5].error + "";
	let t;

	const block = {
		c: function create() {
			span = element("span");
			t = text(t_value);
			this.h();
		},
		l: function claim(nodes) {
			span = claim_element(nodes, "SPAN", { class: true });
			var span_nodes = children(span);
			t = claim_text(span_nodes, t_value);
			span_nodes.forEach(detach_dev);
			this.h();
		},
		h: function hydrate() {
			attr_dev(span, "class", "ml-2 text-red-600");
			add_location(span, file, 160, 8, 4602);
		},
		m: function mount(target, anchor) {
			insert_dev(target, span, anchor);
			append_dev(span, t);
		},
		p: function update(ctx, dirty) {
			if (dirty & /*gist*/ 32 && t_value !== (t_value = /*gist*/ ctx[5].error + "")) set_data_dev(t, t_value);
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(span);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_2.name,
		type: "if",
		source: "(160:6) {#if gist.error}",
		ctx
	});

	return block;
}

// (168:6) {#if !cm}
function create_if_block(ctx) {
	let textarea;
	let mounted;
	let dispose;

	const block = {
		c: function create() {
			textarea = element("textarea");
			this.h();
		},
		l: function claim(nodes) {
			textarea = claim_element(nodes, "TEXTAREA", { class: true, value: true });
			children(textarea).forEach(detach_dev);
			this.h();
		},
		h: function hydrate() {
			attr_dev(textarea, "class", "w-full h-full p-1");
			textarea.value = /*content*/ ctx[4];
			add_location(textarea, file, 168, 6, 4840);
		},
		m: function mount(target, anchor) {
			insert_dev(target, textarea, anchor);

			if (!mounted) {
				dispose = listen_dev(textarea, "input", /*handleInput*/ ctx[9], false, false, false);
				mounted = true;
			}
		},
		p: function update(ctx, dirty) {
			if (dirty & /*content*/ 16) {
				prop_dev(textarea, "value", /*content*/ ctx[4]);
			}
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(textarea);
			mounted = false;
			dispose();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block.name,
		type: "if",
		source: "(168:6) {#if !cm}",
		ctx
	});

	return block;
}

function create_fragment(ctx) {
	let t0;
	let header;
	let t1;
	let div0;
	let a0;
	let t2;
	let t3;
	let main;
	let div1;
	let t4;
	let div8;
	let div2;
	let t5;
	let div7;
	let div3;
	let markmap;
	let updating_mm;
	let updating_el;
	let t6;
	let toolbar;
	let t7;
	let div6;
	let div4;
	let a1;
	let t8;
	let t9;
	let div5;
	let a2;
	let t10;
	let t11;
	let span;
	let t12;
	let t13;
	let footer;
	let current;
	let mounted;
	let dispose;
	header = new Header({ $$inline: true });

	function select_block_type(ctx, dirty) {
		if (!/*gist*/ ctx[5]) return create_if_block_1;
		return create_else_block;
	}

	let current_block_type = select_block_type(ctx);
	let if_block0 = current_block_type(ctx);
	let if_block1 = !/*cm*/ ctx[3] && create_if_block(ctx);

	function markmap_mm_binding(value) {
		/*markmap_mm_binding*/ ctx[15].call(null, value);
	}

	function markmap_el_binding(value) {
		/*markmap_el_binding*/ ctx[16].call(null, value);
	}

	let markmap_props = { content: /*content*/ ctx[4] };

	if (/*mm*/ ctx[2] !== void 0) {
		markmap_props.mm = /*mm*/ ctx[2];
	}

	if (/*svgEl*/ ctx[1] !== void 0) {
		markmap_props.el = /*svgEl*/ ctx[1];
	}

	markmap = new Markmap_1({ props: markmap_props, $$inline: true });
	binding_callbacks.push(() => bind(markmap, "mm", markmap_mm_binding));
	binding_callbacks.push(() => bind(markmap, "el", markmap_el_binding));

	toolbar = new Toolbar_1({
			props: {
				class: "absolute right-0 bottom-0 mr-2 mb-2",
				mm: /*mm*/ ctx[2]
			},
			$$inline: true
		});

	footer = new Footer({ $$inline: true });

	const block = {
		c: function create() {
			t0 = space();
			create_component(header.$$.fragment);
			t1 = space();
			div0 = element("div");
			a0 = element("a");
			t2 = text("NEW: markmap in VSCode!");
			t3 = space();
			main = element("main");
			div1 = element("div");
			if_block0.c();
			t4 = space();
			div8 = element("div");
			div2 = element("div");
			if (if_block1) if_block1.c();
			t5 = space();
			div7 = element("div");
			div3 = element("div");
			create_component(markmap.$$.fragment);
			t6 = space();
			create_component(toolbar.$$.fragment);
			t7 = space();
			div6 = element("div");
			div4 = element("div");
			a1 = element("a");
			t8 = text("Download as interactive HTML");
			t9 = space();
			div5 = element("div");
			a2 = element("a");
			t10 = text("Download as SVG");
			t11 = space();
			span = element("span");
			t12 = text("⚠️");
			t13 = space();
			create_component(footer.$$.fragment);
			this.h();
		},
		l: function claim(nodes) {
			const head_nodes = query_selector_all("[data-svelte=\"svelte-r18bnt\"]", document_1.head);
			head_nodes.forEach(detach_dev);
			t0 = claim_space(nodes);
			claim_component(header.$$.fragment, nodes);
			t1 = claim_space(nodes);
			div0 = claim_element(nodes, "DIV", { class: true, style: true });
			var div0_nodes = children(div0);

			a0 = claim_element(div0_nodes, "A", {
				class: true,
				href: true,
				target: true,
				rel: true
			});

			var a0_nodes = children(a0);
			t2 = claim_text(a0_nodes, "NEW: markmap in VSCode!");
			a0_nodes.forEach(detach_dev);
			div0_nodes.forEach(detach_dev);
			t3 = claim_space(nodes);
			main = claim_element(nodes, "MAIN", { class: true });
			var main_nodes = children(main);
			div1 = claim_element(main_nodes, "DIV", { class: true });
			var div1_nodes = children(div1);
			if_block0.l(div1_nodes);
			div1_nodes.forEach(detach_dev);
			t4 = claim_space(main_nodes);
			div8 = claim_element(main_nodes, "DIV", { class: true });
			var div8_nodes = children(div8);
			div2 = claim_element(div8_nodes, "DIV", { class: true });
			var div2_nodes = children(div2);
			if (if_block1) if_block1.l(div2_nodes);
			div2_nodes.forEach(detach_dev);
			t5 = claim_space(div8_nodes);
			div7 = claim_element(div8_nodes, "DIV", { class: true });
			var div7_nodes = children(div7);
			div3 = claim_element(div7_nodes, "DIV", { class: true });
			var div3_nodes = children(div3);
			claim_component(markmap.$$.fragment, div3_nodes);
			t6 = claim_space(div3_nodes);
			claim_component(toolbar.$$.fragment, div3_nodes);
			div3_nodes.forEach(detach_dev);
			t7 = claim_space(div7_nodes);
			div6 = claim_element(div7_nodes, "DIV", {});
			var div6_nodes = children(div6);
			div4 = claim_element(div6_nodes, "DIV", { class: true });
			var div4_nodes = children(div4);
			a1 = claim_element(div4_nodes, "A", {});
			var a1_nodes = children(a1);
			t8 = claim_text(a1_nodes, "Download as interactive HTML");
			a1_nodes.forEach(detach_dev);
			div4_nodes.forEach(detach_dev);
			t9 = claim_space(div6_nodes);
			div5 = claim_element(div6_nodes, "DIV", { class: true, title: true });
			var div5_nodes = children(div5);
			a2 = claim_element(div5_nodes, "A", {});
			var a2_nodes = children(a2);
			t10 = claim_text(a2_nodes, "Download as SVG");
			a2_nodes.forEach(detach_dev);
			t11 = claim_space(div5_nodes);
			span = claim_element(div5_nodes, "SPAN", { class: true });
			var span_nodes = children(span);
			t12 = claim_text(span_nodes, "⚠️");
			span_nodes.forEach(detach_dev);
			div5_nodes.forEach(detach_dev);
			div6_nodes.forEach(detach_dev);
			div7_nodes.forEach(detach_dev);
			div8_nodes.forEach(detach_dev);
			main_nodes.forEach(detach_dev);
			t13 = claim_space(nodes);
			claim_component(footer.$$.fragment, nodes);
			this.h();
		},
		h: function hydrate() {
			document_1.title = "Try Markmap";
			attr_dev(a0, "class", "inline-block text-sm text-orange-600 underline");
			attr_dev(a0, "href", "https://marketplace.visualstudio.com/items?itemName=gera2ld.markmap-vscode");
			attr_dev(a0, "target", "_blank");
			attr_dev(a0, "rel", "noreferrer noopener");
			add_location(a0, file, 140, 2, 3698);
			attr_dev(div0, "class", "fixed z-10 hidden md:block");
			set_style(div0, "top", "15px");
			set_style(div0, "left", "50%");
			add_location(div0, file, 139, 0, 3629);
			attr_dev(div1, "class", "flex items-center mb-4 text-sm");
			add_location(div1, file, 150, 2, 4015);
			attr_dev(div2, "class", "flex-1 min-w-0 border border-gray-300");
			add_location(div2, file, 166, 4, 4745);
			attr_dev(div3, "class", "markmap-wrapper flex-1");
			add_location(div3, file, 172, 6, 4994);
			add_location(a1, file, 185, 10, 5316);
			attr_dev(div4, "class", "inline-block px-4 py-2");
			add_location(div4, file, 184, 8, 5269);
			add_location(a2, file, 188, 10, 5519);
			attr_dev(span, "class", "text-red-500");
			add_location(span, file, 189, 10, 5592);
			attr_dev(div5, "class", "inline-block px-4 py-2");
			attr_dev(div5, "title", "SVG does not work well with PrismJS and MathJax");
			add_location(div5, file, 187, 8, 5416);
			add_location(div6, file, 183, 6, 5255);
			attr_dev(div7, "class", "flex-1 min-w-0 flex flex-col");
			add_location(div7, file, 171, 4, 4945);
			attr_dev(div8, "class", "flex-1 md:flex min-h-0 repl");
			add_location(div8, file, 165, 2, 4699);
			attr_dev(main, "class", "inner flex flex-col md:fs");
			add_location(main, file, 149, 0, 3972);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t0, anchor);
			mount_component(header, target, anchor);
			insert_dev(target, t1, anchor);
			insert_dev(target, div0, anchor);
			append_dev(div0, a0);
			append_dev(a0, t2);
			insert_dev(target, t3, anchor);
			insert_dev(target, main, anchor);
			append_dev(main, div1);
			if_block0.m(div1, null);
			append_dev(main, t4);
			append_dev(main, div8);
			append_dev(div8, div2);
			if (if_block1) if_block1.m(div2, null);
			/*div2_binding*/ ctx[14](div2);
			append_dev(div8, t5);
			append_dev(div8, div7);
			append_dev(div7, div3);
			mount_component(markmap, div3, null);
			append_dev(div3, t6);
			mount_component(toolbar, div3, null);
			append_dev(div7, t7);
			append_dev(div7, div6);
			append_dev(div6, div4);
			append_dev(div4, a1);
			append_dev(a1, t8);
			append_dev(div6, t9);
			append_dev(div6, div5);
			append_dev(div5, a2);
			append_dev(a2, t10);
			append_dev(div5, t11);
			append_dev(div5, span);
			append_dev(span, t12);
			insert_dev(target, t13, anchor);
			mount_component(footer, target, anchor);
			current = true;

			if (!mounted) {
				dispose = [
					listen_dev(a0, "click", /*onClickNewFeature*/ ctx[8], false, false, false),
					listen_dev(a1, "click", prevent_default(/*onDownloadHTML*/ ctx[7]), false, true, false),
					listen_dev(a2, "click", prevent_default(/*onDownloadSVG*/ ctx[6]), false, true, false)
				];

				mounted = true;
			}
		},
		p: function update(ctx, [dirty]) {
			if (current_block_type === (current_block_type = select_block_type(ctx)) && if_block0) {
				if_block0.p(ctx, dirty);
			} else {
				if_block0.d(1);
				if_block0 = current_block_type(ctx);

				if (if_block0) {
					if_block0.c();
					if_block0.m(div1, null);
				}
			}

			if (!/*cm*/ ctx[3]) {
				if (if_block1) {
					if_block1.p(ctx, dirty);
				} else {
					if_block1 = create_if_block(ctx);
					if_block1.c();
					if_block1.m(div2, null);
				}
			} else if (if_block1) {
				if_block1.d(1);
				if_block1 = null;
			}

			const markmap_changes = {};
			if (dirty & /*content*/ 16) markmap_changes.content = /*content*/ ctx[4];

			if (!updating_mm && dirty & /*mm*/ 4) {
				updating_mm = true;
				markmap_changes.mm = /*mm*/ ctx[2];
				add_flush_callback(() => updating_mm = false);
			}

			if (!updating_el && dirty & /*svgEl*/ 2) {
				updating_el = true;
				markmap_changes.el = /*svgEl*/ ctx[1];
				add_flush_callback(() => updating_el = false);
			}

			markmap.$set(markmap_changes);
			const toolbar_changes = {};
			if (dirty & /*mm*/ 4) toolbar_changes.mm = /*mm*/ ctx[2];
			toolbar.$set(toolbar_changes);
		},
		i: function intro(local) {
			if (current) return;
			transition_in(header.$$.fragment, local);
			transition_in(markmap.$$.fragment, local);
			transition_in(toolbar.$$.fragment, local);
			transition_in(footer.$$.fragment, local);
			current = true;
		},
		o: function outro(local) {
			transition_out(header.$$.fragment, local);
			transition_out(markmap.$$.fragment, local);
			transition_out(toolbar.$$.fragment, local);
			transition_out(footer.$$.fragment, local);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(t0);
			destroy_component(header, detaching);
			if (detaching) detach_dev(t1);
			if (detaching) detach_dev(div0);
			if (detaching) detach_dev(t3);
			if (detaching) detach_dev(main);
			if_block0.d();
			if (if_block1) if_block1.d();
			/*div2_binding*/ ctx[14](null);
			destroy_component(markmap);
			destroy_component(toolbar);
			if (detaching) detach_dev(t13);
			destroy_component(footer, detaching);
			mounted = false;
			run_all(dispose);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function download(filename, url) {
	const a = document.createElement("a");
	a.download = filename;
	a.href = url;
	a.click();
}

function assert(value, message) {
	if (!value) throw new Error(message);
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots("Repl", slots, []);
	let editorEl;
	let svgEl;
	let mm;
	let cm;
	let content = "";
	let gist;
	const { page } = stores$1();

	function onDownloadSVG() {
		const svgHtml = svgEl.outerHTML.replace(/<br>/g, "<br/>");
		download("markmap.svg", "data:image/svg;utf8," + encodeURIComponent(svgHtml));
	}

	function onDownloadHTML() {
		const { root, features } = transform$2(content);
		const assets = getUsedAssets(features);
		const html = fillTemplate(root, assets);
		download("markmap.html", "data:text/html;utf8," + encodeURIComponent(html));
	}

	function update() {
		if (cm) {
			$$invalidate(4, content = cm.getValue());
		}
	}

	function onClickNewFeature() {
		track("event", "click", { event_label: "markmapVSCodeFromREPL" });
	}

	function handleInput(e) {
		$$invalidate(4, content = e.target.value);
	}

	function handleGistInput() {
		$$invalidate(5, gist = {
			url: "https://gist.github.com/gera2ld/af76a4c245b302206b16aec503dbe07b"
		});
	}

	function handleGistOpen() {
		if (!gist) return;
		track("event", "gist", { event_label: gistType });
		let gistType;
		let gistId;

		try {
			const url = new URL(gist.url);

			if (url.origin === "https://gist.github.com") {
				// https://gist.github.com/gera2ld/af76a4c245b302206b16aec503dbe07b
				const parts = url.pathname.split("/");

				assert(parts.length === 3);
				gistId = parts[2];
				gistType = "github";
				const file = url.hash ? `/${url.hash.slice(1)}` : "";
				window.location.hash = `#?gist=${gistId}${file}`;
			} else if (url.origin === "https://gitee.com") {
				// https://gitee.com/gerald/codes/a73wgucr28dhlxp91kytm57
				const parts = url.pathname.split("/");

				assert(parts.length === 4);
				assert(parts[2] === "codes");
				gistId = parts[3];
				gistType = "gitee";
				const file = url.hash ? `/${url.hash.slice(1)}` : "";
				window.location.hash = `#?gist=gitee:${gistId}${file}`;
			} else {
				throw new Error("Unknown service");
			}
		} catch {
			$$invalidate(5, gist.error = "Unrecognized Gist URL", gist);
		}

		if (!gist.error) track("event", "openGist", { event_label: gistType });
	}

	function handleGistCancel() {
		$$invalidate(5, gist = null);
	}

	let revoke;

	onMount(async () => {
		revoke = subscribeHash(text => {
			if (cm) cm.setValue(text);
			$$invalidate(4, content = text);
		});

		const { default: CodeMirror } = await Promise.all([import('./codemirror.c51ae1f8.js'), __inject_styles(["client-ff2bd8be.css","codemirror-8033c53c.css"])]).then(function(x) { return x[0]; });

		$$invalidate(3, cm = CodeMirror(editorEl, {
			lineNumbers: true,
			mode: "markdown",
			value: content,
			indentUnit: 2,
			tabSize: 2
		}));

		cm.setOption("extraKeys", {
			Tab: cm => {
				if (cm.somethingSelected()) {
					cm.execCommand("indentMore");
				} else {
					cm.replaceSelection((" ").repeat(cm.getOption("indentUnit")));
				}
			},
			"Shift-Tab": cm => {
				cm.execCommand("indentLess");
			}
		});

		cm.on("change", lodash_debounce(update, 500));
	});

	onDestroy(() => {
		$$invalidate(3, cm = null);

		if (revoke) {
			revoke();
			revoke = null;
		}
	});

	const writable_props = [];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== "$$") console.warn(`<Repl> was created with unknown prop '${key}'`);
	});

	const input_handler = e => {
		$$invalidate(5, gist.url = e.target.value, gist);
	};

	function div2_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			editorEl = $$value;
			$$invalidate(0, editorEl);
		});
	}

	function markmap_mm_binding(value) {
		mm = value;
		$$invalidate(2, mm);
	}

	function markmap_el_binding(value) {
		svgEl = value;
		$$invalidate(1, svgEl);
	}

	$$self.$capture_state = () => ({
		onMount,
		onDestroy,
		stores: stores$1,
		debounce: lodash_debounce,
		transform: transform$2,
		getUsedAssets,
		fillTemplate,
		track,
		Markmap: Markmap_1,
		Toolbar: Toolbar_1,
		Header,
		Footer,
		subscribeHash,
		editorEl,
		svgEl,
		mm,
		cm,
		content,
		gist,
		page,
		download,
		onDownloadSVG,
		onDownloadHTML,
		update,
		onClickNewFeature,
		handleInput,
		handleGistInput,
		assert,
		handleGistOpen,
		handleGistCancel,
		revoke
	});

	$$self.$inject_state = $$props => {
		if ("editorEl" in $$props) $$invalidate(0, editorEl = $$props.editorEl);
		if ("svgEl" in $$props) $$invalidate(1, svgEl = $$props.svgEl);
		if ("mm" in $$props) $$invalidate(2, mm = $$props.mm);
		if ("cm" in $$props) $$invalidate(3, cm = $$props.cm);
		if ("content" in $$props) $$invalidate(4, content = $$props.content);
		if ("gist" in $$props) $$invalidate(5, gist = $$props.gist);
		if ("revoke" in $$props) revoke = $$props.revoke;
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	return [
		editorEl,
		svgEl,
		mm,
		cm,
		content,
		gist,
		onDownloadSVG,
		onDownloadHTML,
		onClickNewFeature,
		handleInput,
		handleGistInput,
		handleGistOpen,
		handleGistCancel,
		input_handler,
		div2_binding,
		markmap_mm_binding,
		markmap_el_binding
	];
}

class Repl extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, {});

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Repl",
			options,
			id: create_fragment.name
		});
	}
}

export default Repl;

import __inject_styles from './inject_styles.997195c9.js';